// cargo new: creates new directory and project
// cargo init: adds/converts project to cargo, creates Cargo.toml
// cargo build: builds project and creates executable (--release to compile with optimizations)
// cargo run: compiles and runs executable
// cargo check: checks that code compiles, does not produce an executable

fn main() {
    println!("Hello, world!");
}
